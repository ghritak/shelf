import React from 'react';
import { Text, View } from 'react-native';
import { styles } from '../../styles/styles';

const ChartScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.screenHeader}>Chart</Text>
    </View>
  );
};

export default ChartScreen;
